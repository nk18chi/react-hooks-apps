import React from "react";
import { render } from "@testing-library/react";

describe("renders", () => {
  test.todo("renders time list");
  test.todo("renders country name");
  test.todo("renders city name");
  test.todo("renders current time");
  test.todo("renders a remove link");
});

describe("functions", () => {
  test.todo("able to add a time list");
  test.todo("display time info and header.");
  test.todo("able to remove a time list");
  test.todo("undisplay the header table if there is no list");
  test.todo("update the local time by each seconds");
});
